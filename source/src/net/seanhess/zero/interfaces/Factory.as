package net.seanhess.zero.interfaces
{
	/**
	 * Creates a new instance of the implementation every time it is needed
	 * rather than passing the same one
	 */ 
	public class Factory
	{
		public function Factory()
		{
		}
	}
}